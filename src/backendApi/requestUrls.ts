const backendUrl = 'http://localhost:8080';

export const requestUrls = {
    fetchCountries: `${backendUrl}/countries`,
    fetchUsers: `${backendUrl}/users/`,
    fetchRecipes: `${backendUrl}/recipes/`,
}